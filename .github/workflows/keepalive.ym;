name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: curl -s https://proseiq.online/health
